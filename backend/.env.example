# Generic LLM configuration (recommended)
LLM_PROVIDER=anthropic
LLM_API_KEY=replace-with-your-provider-key
LLM_MODEL=replace-with-your-approved-model-id

# Backward-compatible provider-specific variables are still accepted by the backend.
# ANTHROPIC_API_KEY=replace-with-your-provider-key
# ANTHROPIC_MODEL=replace-with-your-approved-model-id

NOTION_TOKEN=secret_...
NOTION_DATABASE_ID=...
MAIDA_PORT=8765

# Explicit browser/native WebView origins allowed to call this API. Keep the
# list narrow in production; never replace it with "*" when credentials are on.
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000","http://localhost","capacitor://localhost"]

# Path of the SQLite file backing the study store. Records survive process
# restarts; point this at a mounted volume in container deployments.
MAIDA_DB_PATH=maida.db

# Demo mode. OFF by default. When on, two presentation-only behaviours are
# enabled: extraction falls back to a clearly-stamped rehearsed record if the
# LLM is unreachable, and POST /api/demo/reset can clear the store. Never
# enable this on a deployment that holds real research data.
MAIDA_DEMO_MODE=false
